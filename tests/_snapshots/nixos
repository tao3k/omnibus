#pretty
{
  bootProfile = {
    speedup = true;
    systemd-boot = {
      enable = true;
    };
    systemd-initrd = {
      enable = false;
    };
  };
  customModuleBootTimeOut = 10;
  hyprland = {
    autoLogin = {
      enable = true;
      shell = "zsh";
    };
    nvidia = true;
    swww = {
      enable = true;
      runtimeEnv = { };
    };
  };
}