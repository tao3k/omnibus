#pretty
{
  makeScript = <function, args: {aliases?, entrypoint, globalState?, help?, name, persistState?, replace?, searchPaths?}>;
  scripts = {
    example = <derivation runScriptWithEnv>;
    makeTemplate = <derivation example>;
    pogTest = <derivation tool-name-0.0.0>;
  };
}